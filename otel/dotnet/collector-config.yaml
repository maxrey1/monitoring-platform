receivers:
  otlp:
    protocols:
      grpc:
      http:

processors:
  batch: {}

  resource:
    attributes:
      - key: service.name
        value: dotnet-service
        action: insert

exporters:
  otlp:
    endpoint: hyperdx:4317
    tls:
      insecure: true

  prometheus:
    endpoint: "0.0.0.0:8889"

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlp]

    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [prometheus]
